#Get the first digit
a = int(input('Enter leftmost digit: '))

#Get the second digit
b = int(input('Enter the next digit: '))

#Get the third digit
c = int(input('Enter the next digit: '))

#Get the fourth digit
d = int(input('Enter the next digit: '))

#Decimal conversion of binary numbers and output result
print('The value is',8*a+4*b+2*c+1*d)
